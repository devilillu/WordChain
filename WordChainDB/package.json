{
  "name": "wordchaindb",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist && npx tsc --sourceMap --rootDir src --outDir dist",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [],
  "author": "nomant",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "kafkajs": "^2.2.4",
    "mongo": "^0.1.0",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "@types/kafkajs": "^1.8.2",
    "@types/rimraf": "^3.0.2",
    "typescript": "^5.7.3"
  }
}
